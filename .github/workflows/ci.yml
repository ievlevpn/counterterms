name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - name: Lint the library (ruff)
        run: uv run ruff check counterterms
      - name: Run tests
        run: uv run pytest -q
